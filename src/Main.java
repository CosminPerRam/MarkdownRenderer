import gui.GUI;

/**
 * Our program entry point.
 */
public class Main {
    /**
     * The function that always runs the first.
     * @param args The CLI-provided arguments.
     */
    public static void main(String[] args) {
        GUI.initialize();
    }
}
